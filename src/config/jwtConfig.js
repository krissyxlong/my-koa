const client_id = 'client_id';
const client_secret = 'client_secret';
const grant_type = '';
const login_system = 'KUS FUM'; // 判断逻辑？
const tokoenSecret = 'hello'; // todo 公钥加密？

module.exports = {
    client_id,
    client_secret,
    grant_type,
    login_system,
    tokoenSecret
}